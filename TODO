
-- GATEKEEPER
add schema ownership validation to check that a schema has not been assigned ownership
to more than one user.


-- SCOPE
add user ownership grouping to DbCatalog object
proper formatting of table constraints

-- TERM


-- MIGRATE

-- DEPLOYMENT
set proper __version__ when deployment happens
make proper circle ci file
